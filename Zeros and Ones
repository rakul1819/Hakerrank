import numpy

# Read the space-separated integers as a list and convert to a tuple
shape = tuple(map(int, input().split()))

# Print the zeros array with integer type
print(numpy.zeros(shape, dtype=int))

# Print the ones array with integer type
print(numpy.ones(shape, dtype=int))
