import numpy

# Read the coefficients as a list of floats
coeffs = list(map(float, input().split()))

# Read the value of x
x = float(input())

# Evaluate the polynomial at x and print the result
print(numpy.polyval(coeffs, x))
