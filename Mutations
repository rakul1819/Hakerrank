def mutate_string(string, position, character):
    # Slice from start to position + new character + slice from position+1 to end
    return string[:position] + character + string[position + 1:]



    return

